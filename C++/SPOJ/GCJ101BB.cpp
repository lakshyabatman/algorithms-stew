#include<iostream>
#include<vector>
using namespace std;


int main() {
  int test;
  cin >> test;
  while(test--) {
    int,n,k,b,t;
    int res = 0;
    cin>> n >> k >> b >> t;
    vector<int> position(n,0);
    vector<int> speed(n,0);
    for(int i =0;i<n;i++) {
      cin >> position[i];
    }
    for(int i=0;i<n;i++) {
      cin >> speed[i];
    }
    
    
    

  }

  return 0;
}